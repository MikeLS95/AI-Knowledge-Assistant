<div class="w-full">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold">Dashboard</h1>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: 'bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded-lg transition' %>
  </div>
  
  <div class="bg-slate-800 rounded-lg p-6 border border-slate-700">
    <h2 class="text-2xl font-semibold text-white mb-4">Welcome, <%= current_user.email %>!</h2>
    <p class="text-slate-300">This is your dashboard. More features coming soon.</p>
  </div>
</div>